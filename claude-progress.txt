Session 1 — Initializer

Scaffolded project per ~/.claude/bg-tasks/long-run-task.md.
- CLAUDE.md describing layout, startup sequence, end-of-session rules.
- feature_list.json with decomposed core-surface tasks.
- init.sh smoke-tests with `go build`, `go vet`, `go test` once go.mod exists.
- No feature code written. Awaiting "resume long run" to begin task 1.

Reference SDK: /Users/goubo/projects/azure-sdk-for-js/sdk/ai/ai-projects
TS public API: src/index.ts exports
Samples: samples-dev/<group>/<name>Basics.ts

--- Session 2 ---
Feature: full-smoke (and all 32 prior tasks)
What I did: Implemented every task in feature_list.json from go-module-init through full-smoke. Built azaiprojects.Client with bearer auth and v1 api-version, plus four operation groups (connections, deployments, indexes, datasets) each with their own NewClient/NewClientFromPipeline and unit tests using fake transporters. AIProjectClient.Connections/Deployments/Indexes/Datasets accessors share the parent pipeline. examples/{connections,deployments,indexes,datasets} compile and mirror the TS samples-dev/*Basics.ts programs.
Next: none — all tasks pass.
Issues: none. A few tasks were combined into single commits where they were small and shared a file (deployments-list+get, indexes-list-versions+get+CoU+delete, datasets client). Bytes-of-work-per-commit slightly above the strict one-task-per-commit ideal in those spots; future sessions should split unless deliberately bundling.

--- Session 3 ---
Feature: agents (full operation group)
What I did: Ported the entire @azure/ai-projects classic/agents surface to Go as a new azaiprojects/agents package. 12 operations (List/ListVersions/Get/GetVersion/Create/Update/CreateVersion/Delete/DeleteVersion/CreateFromManifest/UpdateFromManifest/CreateVersionFromManifest), models for Agent/AgentVersion/AgentDefinition base + Prompt/Hosted/Workflow variants with kind-discriminated dispatch, AgentEndpoint/AgentCard/AgentBlueprintReference/Delete responses, cursor-based pagination shapes (data + has_more + last_id — no next_link). foundryFeatures header gets the literal ",AgentEndpoints=V1Preview" suffix appended per the TS reference. project.Agents() accessor wired. examples/agents mirrors agentCurd.ts (CreateVersion x2 -> GetVersion -> Get -> ListVersions -> DeleteVersion x2). Per the pragmatic-full-surface scope, deeply-nested unions (Tools, ToolChoice, VersionSelector, AuthorizationSchemes) stay as json.RawMessage pass-throughs — caller decodes when needed.
Next: none — all 10 agents tasks pass; full repo suite green across 7 test packages.
Issues: stray "agents" binary briefly committed in c4 (ba5d386); amended out and added /agents,/connections,/datasets,/deployments,/indexes to .gitignore so this can't recur from `go build ./examples/<dir>/...`.

--- Session 4 ---
Feature: evaluationrules (full operation group)
What I did: Ported @azure/ai-projects classic/evaluationRules to Go as a new azaiprojects/evaluationrules package. 4 ops (List/Get/CreateOrUpdate/Delete) following the standard {value,nextLink} pagination shape from internal/shared.PageResponse. Models: EvaluationRule + EvaluationRuleAction discriminated union (Continuous/HumanPreview + base fallback) wrapped in EvaluationRuleActionValue dispatcher (mirrors AgentDefinitionValue pattern). CreateOrUpdate is PUT and accepts both 200 and 201; Delete expects 204 with no body. project.EvaluationRules() accessor wired. examples/evaluationrules walks CreateOrUpdate -> Get -> List -> Delete using a pre-existing eval ID supplied via FOUNDRY_EVAL_ID (the TS sample's OpenAI passthrough is out of scope).
Next: none — all 5 evaluationrules tasks pass; full repo suite green across 8 test packages.
Issues: none.
